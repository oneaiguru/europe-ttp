# Weaver Behave Smoke Test

Goal: prove Python2 + behave are available inside the Weaver container.

Do exactly this:
1) Run `python2 --version` and `behave --version`.
2) Create `docs/WEAVER_BEHAVE_OK.md` with:
   - UTC timestamp
   - python2 version output
   - behave version output
3) Do not modify any other files.
